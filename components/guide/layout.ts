/**
 * The reading column inside a guide page. Prose is held to a comfortable
 * measure while the video breaks out to the full width of the main region, so
 * both the shell and the video stage need these paddings to line up.
 */
export const guideColumn =
  "mx-auto w-full max-w-[46rem] px-5 sm:px-8 xl:px-10";
