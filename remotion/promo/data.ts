// Promo Scene — intro beat only, landscape (16:9).
// Twiga logo reveal → hero app-logo + title → shrinks into the WhatsApp icon's
// corner as the unread-message badge → slides behind the icon and is gone.
// Logo asset: public/promo/twiga-logo.png (1024², circle inscribed).

// 16:9 desktop canvas.
export const WIDTH = 1920;
export const HEIGHT = 1080;
export const FPS = 30;
// Reveal → title → morph → badge melts into the icon → icon lifts → the closing
// line types itself out → still hold on the finished poster.
export const DURATION = 10.5;
